<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The Inject feature's GET (fetch the injected value) and POST (send
         Sum/Peek results) both need this — without it, HttpURLConnection
         fails with a SecurityException/UnknownHostException-style
         permission denial before ever reaching the network. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Needed on Android 11+ (API 30+) package visibility to actually see
         other installed apps' launcher entries/icons — without this,
         queryIntentActivities() silently returns almost nothing and the
         "choose an app icon" picker would appear empty. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <!-- Same package-visibility need, but for AppWidgetManager's
             getInstalledProviders() — without this the widget picker
             would come back empty/filtered on Android 11+. -->
        <intent>
            <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
        </intent>
        <!-- Needed so the ambient screen's camera quick-action button can
             actually resolve and launch the system camera app on Android
             11+ — without a matching <queries> entry, startActivity() for
             this implicit intent silently fails to find any app to handle
             it, package-visibility rules block it from seeing the camera
             app the same way they'd block seeing any other app's launcher
             entry. -->
        <intent>
            <action android:name="android.media.action.STILL_IMAGE_CAMERA" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.NotesNative">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.NotesNative.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
